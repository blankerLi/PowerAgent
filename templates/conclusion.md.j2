{#
  结论边界声明。这个模板由 `report.render.render_conclusion()` 单独渲染，
  它的命名空间**只有** `ReportContext.conclusion` 一个映射——过程埋点、
  运行统计、被拒计数都不在这里，写了也取不到值。这是 R17 AC11 第 3 条
  「过程量不进入结论段渲染上下文」的构造实现，见 report/context.py 的
  模块 docstring。

  两个分支的文本都是固定表述，不由自由文本拼接产生（R1 AC6）。两个分支
  都不含实物可行性的措辞——工程轨分支同样不含，因为「报告是否断言实物
  可行」不该取决于走了哪个分支（R1 AC6 明确要求模板中不存在那类表述的
  渲染路径，是构造保证而非事后校验）。
#}
{% if in_progress %}
> **任务进行中 · 非终态。** 本报告在任务尚未记录停止原因时渲染，所载结论可能随后续轮次变化。
{% endif %}
{% if simulation_only %}
本报告的结论限定于冻结的仿真参考模型。所载数值全部来自该模型在冻结场景集上的运行记录，用于回答「在这个假设电路上，搜索方法是否有效」，不构成器件选型或制造依据。

本任务未输出工程推荐，原因为三项：校准参数置信区间为空；Robustness 第三维未执行；结论限定于冻结仿真参考模型。
{% else %}
本报告依据工程轨配置渲染。所载数值来自冻结场景集上的运行记录与已登记的校准记录，其效力范围以该校准记录声明的适用条件为界。
{% endif %}

- 任务类型：`{{ task_kind }}`
- 停止原因：`{{ stop_reason }}`{% if cause %} · 伴随原因 `{{ cause }}`{% endif %}



{% if has_feasible_candidate %}
在冻结场景集的评价层上，存在满足全部硬约束且跑完整个评价集的候选。其中主目标
（`{{ objective_metric_id }}`）取值最优的一个为 `{{ best_candidate_id }}`，其参数为
{{ best_parameters | kvline }}。

该候选在评价集上的 worst-case 主目标取值为 {{ best_objective | fmt }}{% if objective_target is not none %}，任务写下的达标线为 {{ objective_target | fmt }}{% endif %}。

采纳与否是 Checkpoint 3 的人工决定，本报告只提供依据，不给出采纳结论。
{% else %}
在冻结场景集的评价层上，不存在满足全部硬约束且跑完整个评价集的候选。可审计的最小
结论内容见「无可行候选时的可审计结论」一节。
{% endif %}
