# 复制为 .env 后填入真实值；.env 已被 .gitignore 排除，凭据不入库。
# 代码只从环境变量读取该 Key（design.md §14），不从 YAML、数据库或 artifacts/ 读。

POWERAGENT_LLM_API_KEY=
POWERAGENT_LLM_BASE_URL=https://api.deepseek.com
POWERAGENT_LLM_MODEL=deepseek-chat
