# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.matlab_engine_venv/

# 测试与覆盖率
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# 运行期产物（唯一事实源是 SQLite，产物可重建）
runs/
artifacts/*
# 例外：这两份是开发期一次性产出的证据资产，不是可重建的运行期产物，且各自被
# preflight 校验（裕量记录比对 metrics.yaml 里的 sha256，双模型记录比对其内嵌的
# model_package_hash）。不入库则任何人 clone 后都无法通过 preflight。
!artifacts/margin_cross_check.json
!artifacts/dual_model_consistency.json
*.db
*.db-journal
*.sqlite

# 凭据：API Key 只从环境变量读取，任何 .env 都不得入库
.env
.env.*
!.env.example

# MATLAB / Simulink
*.slxc
slprj/
*.autosave
*.asv

# 编辑器
.idea/
.vscode/
.DS_Store
