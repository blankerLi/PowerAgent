# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.matlab_engine_venv/

# 测试与覆盖率
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# 运行期产物（唯一事实源是 SQLite，产物可重建）
runs/
artifacts/*
# 例外：这两份是开发期一次性产出的证据资产，不是可重建的运行期产物，且各自被
# preflight 校验（裕量记录比对 metrics.yaml 里的 sha256，双模型记录比对其内嵌的
# model_package_hash）。不入库则任何人 clone 后都无法通过 preflight。
!artifacts/margin_cross_check.json
!artifacts/dual_model_consistency.json
# 真实 LLM 寻优的测量记录。与上面两份不同，它不是确定性证据（重跑数字会变，
# 也带时间戳），preflight 不读它。入库的理由是它承载项目的实测表现数据，
# 别人 clone 后无需自备 API Key 也能看到这条链路跑出过什么结果。
!artifacts/llm_search_report.json
*.db
*.db-journal
*.sqlite

# 凭据：API Key 只从环境变量读取，任何 .env 都不得入库
.env
.env.*
!.env.example

# MATLAB / Simulink
*.slxc
slprj/
*.autosave
*.asv

# 编辑器
.idea/
.vscode/
.DS_Store
